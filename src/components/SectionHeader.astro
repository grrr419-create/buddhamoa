---
type Props = {
  eyebrow: string;
  title: string;
};

const { eyebrow, title } = Astro.props;
---

<div class="section-head">
  <div>
    <p class="eyebrow">{eyebrow}</p>
    <h2>{title}</h2>
  </div>
  <slot />
</div>
